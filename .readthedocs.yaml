version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    
sphinx:
  configuration: src/docs/conf.py

python:
  install:
    - requirements: requirements.txt

# Build PDF & ePub
formats:
  - epub
  - pdf
